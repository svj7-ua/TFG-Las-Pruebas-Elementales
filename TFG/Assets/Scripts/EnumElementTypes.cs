public enum EnumElementTypes
{
    None = -1, // Represents no element
    Poison = 0, // Represents poison element
    Fire = 1, // Represents fire element
    Lightning = 2, // Represents ice element
    Wind = 3, // Represents wind element
    Arcane = 4 // Represents arcane element

    // Add more elements as needed
}